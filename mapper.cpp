#include <thread>
#include <iostream>
#include <vector>
#include <string>

using namespace std;

int createthreads(vector<thread> &threads, int num){
    for(int i=0; i<num; i++){
    
    }

}

int main(){
    
    vector<thread> threads;
    createthreads(threads);
    t1.join();
    t2.join();
    return 0;
}

